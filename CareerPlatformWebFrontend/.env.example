# Base URL for the backend API used by the frontend (must include the /api/v1 prefix)
# For same-origin deployments behind a reverse proxy:
REACT_APP_API_URL=/api/v1

# For local development against the backend running on http://localhost:3001:
# REACT_APP_API_URL=http://localhost:3001/api/v1
