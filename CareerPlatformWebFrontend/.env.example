# Frontend environment variables for local/dev
# If you set REACT_APP_API_URL, include the /api/v1 suffix so requests go to FastAPI's versioned routes.
# If omitted, the app falls back to a relative /api/v1 path and uses CRA dev proxy from package.json.
REACT_APP_API_URL=http://localhost:3001/api/v1
